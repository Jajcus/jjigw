- handling of received QUIT and NICK messages (for proper nickname synchronization)
- IRC stream error handling
- IRC-nick to user-JID mapping
- MUC support (so channel operators etc. are shown)
- Nick/channel name escaping (they could contain characters disallowed in JIDs)
- "private" chats
- simple ident server with "uplink" capability
